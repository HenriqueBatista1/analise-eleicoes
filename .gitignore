# Virtual Environments (Python)
.venv/
venv/
env/

# Python Cache and Compiled Files
**/__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.poetry/

# Environment Variables (Security)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# Service account keys and other secrets (never commit credentials)
secrets/
*service_account*.json
*credentials*.json

# Frontend Node / React
node_modules/
dist/
dist-ssr/
.eslintcache
.yarn-clean

# IDEs and Editors
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Local data artifacts. The pipeline now publishes straight to Google Sheets,
# so these folders are not versioned; ignore them if recreated locally.
dados-brutos/
dados-processados/

# OS Files
.DS_Store
Thumbs.db
